apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helpers.helpers.fullname" . }}-test-connection"
  labels:
    {{- include "helpers.helpers.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helpers.helpers.fullname" . }}:{{ .Values.service.ports }}']
  restartPolicy: Never
